import { defaultCardData } from '../../../components';

export const defaultData = {
  title: 'Tecnologies',
  technologies: [
    { ...defaultCardData },
    { ...defaultCardData },
    { ...defaultCardData }
  ]
}